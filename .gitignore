node_modules
dist
*.log
.DS_Store
.vite

# Extension signing private key — never commit
key.pem
*.der

# Native host wrapper — generated per-machine by install.sh (absolute paths)
native-host/reviewer-host.sh

# Chrome Web Store ZIP artifacts
web-store/

# Claude Code local settings (machine-specific permissions)
.claude/settings.local.json

# Vitest coverage output
coverage/
